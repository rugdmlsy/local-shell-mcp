[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "local-shell-mcp"
version = "4.2.0+morrow.17"
description = "A ChatGPT-compatible OAuth MCP server that gives AI coding agents controlled shell, filesystem, remote-worker, and Playwright access inside a local container."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "local-shell-mcp contributors" }]
keywords = ["mcp", "chatgpt", "shell", "playwright", "coding-agent"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Web Environment",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
]
dependencies = [
  "mcp>=1.27.0,<2",
  "fastapi>=0.111.0",
  "uvicorn[standard]>=0.30.0",
  "pydantic>=2.7.0",
  "pydantic-settings>=2.3.0",
  "PyYAML>=6.0.1",
  "PyJWT>=2.8.0",
  "httpx>=0.27.0",
  "h2>=4.1.0",
  "aiofiles>=23.2.1",
  "python-multipart>=0.0.9",
  "pathspec>=0.12.1",
  "zstandard>=0.23.0,<1",
  "pillow>=10.3.0",
  "playwright>=1.44.0",
  "pywinpty>=2.0.13; platform_system == \"Windows\"",
]

[project.optional-dependencies]
dev = [
  "coverage[toml]>=7.8.0",
  "pytest>=8.2.0",
  "pytest-asyncio>=0.23.0",
  "ruff>=0.5.0",
]
redis = [
  "redis>=5.0.0,<7",
]
s3 = [
  "boto3>=1.34.0,<2",
]
docs = [
  "mkdocs-material>=9.5",
  "mkdocs-static-i18n>=1.3",
]

[project.urls]
Homepage = "https://github.com/rugdmlsy/local-shell-mcp"
Repository = "https://github.com/rugdmlsy/local-shell-mcp"

[project.scripts]
local-shell-mcp = "local_shell_mcp.main:main"
lsm = "local_shell_mcp.main:main"

[tool.hatch.build.targets.wheel]
packages = ["src/local_shell_mcp"]

[tool.hatch.build.hooks.custom]
path = "hatch_build.py"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.coverage.run]
branch = true
parallel = true
sigterm = true
source = ["local_shell_mcp"]

[tool.coverage.report]
fail_under = 93
precision = 1
show_missing = true
skip_covered = true
exclude_also = [
  "if TYPE_CHECKING:",
  "if __name__ == .__main__.:",
  "raise NotImplementedError",
]

[tool.coverage.xml]
output = "coverage.xml"

[tool.coverage.json]
output = "coverage.json"
